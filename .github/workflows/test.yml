name: Test GCD Script

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run test.sh
        run: |
          chmod +x gcd.sh test.sh
          ./test.sh
